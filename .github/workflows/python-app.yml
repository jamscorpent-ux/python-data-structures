name: Docker Build & Test

on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Build Docker Image
        run: docker build -t hash-table-app .
        
      - name: Run Tests in Docker
        run: docker run --rm hash-table-app
